'use strict';

describe('Controller: WolframCtrl', function () {

  beforeEach(module('ipallor'));

  var WolframCtrl;

  beforeEach(inject(function ($controller) {
    WolframCtrl = $controller('WolframCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
