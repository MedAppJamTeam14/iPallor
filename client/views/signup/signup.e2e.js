'use strict';

describe('signup route', function () {

  beforeEach(function () {
    browser.get('/signup');
  });

  it('should have a basic content', function () {
    expect(1).toBe(1);
  });

});
