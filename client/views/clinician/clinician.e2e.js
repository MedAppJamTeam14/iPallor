'use strict';

describe('clinician route', function () {

  beforeEach(function () {
    browser.get('/clinician');
  });

  it('should have a basic content', function () {
    expect(1).toBe(1);
  });

});
